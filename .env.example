# Browser Maps key — repo ROOT .env only (not backend/.env). Restart Vite after edits.
# Cloud Console: enable Maps JavaScript API + Places API (New); restrict by HTTP referrer http://localhost:5173/*
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
